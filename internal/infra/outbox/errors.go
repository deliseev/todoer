package outbox

import "errors"

// errMissingDependency — доставщика собрали без обязательной зависимости.
//
// Неэкспортированная: по ней ветвится только тест этого пакета, а наружу
// такое обещание не выдано — вызывающему довольно того, что конструктор
// вернул ошибку. Понадобится снаружи — экспортировать успеется, обратно
// уже нет.
var errMissingDependency = errors.New("outbox: missing dependency")
